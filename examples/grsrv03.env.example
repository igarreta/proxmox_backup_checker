# SMTP Configuration Example
# Copy to ~/etc/smtp.env and configure with your settings

# SMTP server settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Email addresses
FROM_EMAIL=your-backup-monitor@gmail.com
TO_EMAIL=admin@example.com

# SMTP authentication (use app password for Gmail)
SMTP_TOKEN=your-16-character-app-password