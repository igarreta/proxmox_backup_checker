# Pushover Configuration Example  
# Copy to ~/etc/pushover.env and configure with your settings

# Pushover API credentials (both exactly 30 characters)
PUSHOVER_TOKEN=your-30-character-app-token-here
PUSHOVER_USER=your-30-character-user-key-here

# How to get credentials:
# 1. Create account at https://pushover.net
# 2. Create new application to get PUSHOVER_TOKEN
# 3. Find your user key in account settings for PUSHOVER_USER